from setuptools import setup

setup(name='fdtd_env',
      version='0.0.1',
      author='Renjie Li',
      description='FDTD environment for optimizing photonic crystals by RL',
      license='MIT',
      install_requires=['gym>=0.2.3']
      )
